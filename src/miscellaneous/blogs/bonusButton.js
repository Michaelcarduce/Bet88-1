// import React from "react";
// import PropTypes from "prop-types";

// const BonusButton = ({ className, text, link }) => {
//   // Use a base class for all buttons
//   const baseClass = "bonusButton";

//   return (
//     <a
//       href={link || BonusButton.defaultProps.link}
//       className={`${baseClass} ${className ? className + " " : ""}`}
//       target="_blank"
//       rel="noopener noreferrer">
//       {text || BonusButton.defaultProps.text}
//     </a>
//   );
// };

// BonusButton.propTypes = {
//   className: PropTypes.string,
//   text: PropTypes.string,
//   link: PropTypes.string,
// };

// BonusButton.defaultProps = {
//   text: "Claim Bonus", // Default button text
//   link: "https://example.com", // Default button link
// };

// export default BonusButton;
